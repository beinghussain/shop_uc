import React from "react";
import UnderConstruction from "../lib";

const App = () => (
  <UnderConstruction
    background={{
      textColor: "#fff",
      overlay: {
        color: "#1ABC9C",
        opacity: "1"
      }
    }}
  />
);

export default App;
